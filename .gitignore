node_modules/
dist/
.astro/
*.log
.DS_Store

# секреты — никогда не коммитим
.env
.env.*
!.env.example

# исходные материалы и результаты парсинга
врач/
врач 2/
docs/parsed_doctors/

# Репозиторий публичный, поэтому внутренние материалы клиники сюда не коммитим:
# ТЗ, «понимание задачи», схема интеграции с CRM, промпты фото и иллюстраций,
# макеты токенов. Они живут в приватном репозитории ktm-clinic-design.
# Исключение — выгрузки текста страниц: это дословный текст самого сайта,
# он и так публичен, а на нём завязано правило «сверстал вкладку → npm run texts».
#
# НЕ ЗАМЕНЯТЬ этот файл копией из приватного репозитория: там блока ниже нет,
# и внутренние материалы уедут в открытый доступ (уже случалось 03.08.2026).
docs/*
!docs/тексты-страниц/
!docs/тексты-главной.md
